---
phase: 02-interface-streamlit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app.py
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "app.py carrega dados via get_or_process_data() sem importar data_loader ou geo_engine diretamente"
    - "Sidebar mostra 4 selectboxes em cascata: Data → VP → CMT (opcional) → Motorista (opcional)"
    - "Selecionar Data restringe VP às viaturas escaladas naquele dia"
    - "Selecionar VP restringe CMT e Motorista aos valores daquela VP naquele dia"
    - "CMT='Todos' e Motorista='Todos' não filtram o resultado (opcionais per D-03)"
    - "Botão 'Recarregar Banco de Dados' chama delete_cache() + st.rerun()"
    - "Estado inicial (antes de Data+VP) exibe instrução 'Selecione uma Data e VP na sidebar para visualizar a rota.'"
    - "Banco ausente → get_or_process_data() dispara processamento automático com barra de progresso (D-10)"
  artifacts:
    - path: "app.py"
      provides: "Entry point Streamlit com filtros em cascata"
      min_lines: 55
  key_links:
    - from: "app.py sidebar"
      to: "df filtrado (filtered_df)"
      via: "df_by_date → df_by_vp → df_by_cmt → filtered_df"
      pattern: "df_by_date = df\\[df\\[.Data.\\]"
    - from: "app.py"
      to: "cache_manager"
      via: "get_or_process_data() + delete_cache()"
      pattern: "from cache_manager import get_or_process_data, delete_cache"
---

<objective>
Criar app.py — entry point Streamlit com filtros em cascata e estrutura de estados (sem o mapa ainda, que vem no Plan 03).

Purpose: Estabelecer o esqueleto completo do app: page config, carregamento de dados, sidebar com 4 filtros em cascata, lógica de empty-state, e placeholder para o mapa/tabela que o Plan 03 vai preencher.

Output: app.py funcional que inicia sem erro, exibe sidebar com filtros operacionais, e mostra instrução de texto no estado inicial.
</objective>

<execution_context>
@C:/Users/hdusa/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hdusa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-interface-streamlit/02-CONTEXT.md
@.planning/phases/02-interface-streamlit/02-RESEARCH.md
@.planning/phases/02-interface-streamlit/02-PATTERNS.md

<interfaces>
<!-- Contratos que app.py consome -->

De cache_manager.py (único ponto de entrada per D-02 Phase 1):
```python
def get_or_process_data() -> pd.DataFrame:
    """Retorna DataFrame com 10 colunas: VP, CMT, Motorista, Data, Horário, Data_Hora, Endereco, Latitude, Longitude, Status.
    Se banco inexistente: processa com st.progress() e salva. Se banco existe: lê direto.
    DataFrame vazio (schema correto) retornado se nenhum arquivo .xls encontrado."""

def delete_cache() -> None:
    """Apaga dados_processados.db sem erro se inexistente."""
```

De geo_engine.py (para o polígono — usado no Plan 03, mas app.py já carrega):
```python
def load_perimeter() -> Polygon:
    """Retorna shapely.Polygon do perimetro.kml."""
```

Schema do DataFrame:
```python
# Colunas: VP, CMT, Motorista, Data, Horário, Data_Hora, Endereco, Latitude, Longitude, Status
# Data: str (dia do turno)
# VP: str (ex: "VP-001")
# CMT: str
# Motorista: str
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar app.py com page config, carregamento de dados e filtros em cascata</name>
  <files>app.py</files>

  <read_first>
    - cache_manager.py — assinatura exata de get_or_process_data() e delete_cache(); padrão de imports
    - geo_engine.py — assinatura de load_perimeter()
    - .planning/phases/02-interface-streamlit/02-PATTERNS.md — seção "app.py", código completo dos filtros
    - .planning/phases/02-interface-streamlit/02-RESEARCH.md — Pattern 1 (cascade filters), Pitfall 2 (cascade inválida), Pitfall 3 (empty DataFrame crash)
    - .planning/phases/01-pipeline-de-dados-e-cache/01-03-SUMMARY.md — interface exata de cache_manager
  </read_first>

  <action>
Criar app.py com a seguinte estrutura:

**Imports (ordem: stdlib → third-party → local):**
```python
from pathlib import Path

import pandas as pd
import streamlit as st
from streamlit_folium import st_folium

from cache_manager import delete_cache, get_or_process_data
from geo_engine import load_perimeter
from map_renderer import render_map
```

**Page config (PRIMEIRA linha executável, antes de qualquer st.*):**
```python
st.set_page_config(page_title="VP-GPS — Rastreador de Viaturas", layout="wide")
```

**Título:**
```python
st.title("Rastreador de Viaturas — VP-GPS")
```

**Carregamento de dados e polígono:**
```python
polygon = load_perimeter()
df = get_or_process_data()
```

**Guard de DataFrame vazio** (evitar crash em df["Data"].unique() com df vazio — Pitfall 3):
```python
if df.empty:
    st.warning("Nenhum dado processado. Verifique os arquivos em `dados_vps/` e clique em **Recarregar Banco de Dados**.")
    st.stop()
```

**Sidebar com filtros em cascata:**
```python
with st.sidebar:
    st.header("Filtros")

    if st.button("Recarregar Banco de Dados", use_container_width=True):
        delete_cache()
        st.rerun()

    st.divider()

    # Nível 1: Data
    available_dates = sorted(df["Data"].unique())
    selected_date = st.selectbox("Data", options=available_dates)

    # Nível 2: VP — apenas VPs escaladas na data selecionada (D-02)
    df_by_date = df[df["Data"] == selected_date]
    available_vps = sorted(df_by_date["VP"].unique().tolist())
    selected_vp = st.selectbox("VP", options=available_vps)

    # Nível 3: CMT — opcional (D-03), restrito à VP selecionada
    df_by_vp = df_by_date[df_by_date["VP"] == selected_vp]
    available_cmts = sorted(df_by_vp["CMT"].unique().tolist())
    selected_cmt = st.selectbox("CMT (opcional)", options=["Todos"] + available_cmts)

    # Nível 4: Motorista — opcional (D-03), restrito ao CMT selecionado
    if selected_cmt == "Todos":
        df_by_cmt = df_by_vp
    else:
        df_by_cmt = df_by_vp[df_by_vp["CMT"] == selected_cmt]

    available_motoristas = sorted(df_by_cmt["Motorista"].unique().tolist())
    selected_motorista = st.selectbox(
        "Motorista (opcional)", options=["Todos"] + available_motoristas
    )

    if selected_motorista == "Todos":
        filtered_df = df_by_cmt
    else:
        filtered_df = df_by_cmt[df_by_cmt["Motorista"] == selected_motorista]
```

**Área principal — estado inicial (D-01):**
Neste plano, colocar apenas o placeholder de estado inicial. O mapa e a tabela são integrados no Plan 03.

```python
# --- Main area: placeholder (Plan 03 will replace this block) ---
if selected_date and selected_vp:
    # TODO Plan 03: render map + OUTSIDE table
    st.info(f"VP **{selected_vp}** selecionada — mapa será exibido aqui (Plan 03).")
else:
    st.info("Selecione uma Data e VP na sidebar para visualizar a rota.")
```

**Regra crítica:** NÃO usar `st.session_state` para armazenar valores dos selectboxes — o re-run model do Streamlit gerencia cascade reset automaticamente (Pitfall 2 em RESEARCH.md).

**Regra crítica:** NÃO chamar `load_perimeter()` ou qualquer função de `geo_engine` dentro de `map_renderer.py` — o polígono é passado como argumento.
  </action>

  <verify>
    <automated>cd C:/Users/hdusa/Documents/Github/vp-gps && python -c "import ast; ast.parse(open('app.py').read()); print('syntax OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('app.py').read()); print('syntax OK')"` imprime "syntax OK"
    - `grep -n "from cache_manager import" app.py` retorna linha com `get_or_process_data, delete_cache`
    - `grep -n "from geo_engine import load_perimeter" app.py` retorna ao menos 1 linha
    - `grep -n "from map_renderer import render_map" app.py` retorna ao menos 1 linha
    - `grep -n "from streamlit_folium import st_folium" app.py` retorna ao menos 1 linha
    - `grep -n "import data_loader\|from data_loader" app.py` retorna vazio (proibido — D-02 Phase 1)
    - `grep -n "st.set_page_config" app.py` retorna ao menos 1 linha
    - `grep -n "layout=\"wide\"" app.py` retorna ao menos 1 linha
    - `grep -n "df_by_date = df\[df\[" app.py` retorna ao menos 1 linha (cascade nível 2)
    - `grep -n "df_by_vp = df_by_date\[" app.py` retorna ao menos 1 linha (cascade nível 3)
    - `grep -n "df_by_cmt" app.py` retorna ao menos 2 linhas (definição + uso)
    - `grep -n "Recarregar Banco de Dados" app.py` retorna ao menos 1 linha
    - `grep -n "delete_cache()" app.py` retorna ao menos 1 linha
    - `grep -n "st.rerun()" app.py` retorna ao menos 1 linha
    - `grep -n "st.session_state" app.py` retorna vazio (proibido para filtros em cascata)
    - `grep -n "CMT (opcional)" app.py` retorna ao menos 1 linha
    - `grep -n "Motorista (opcional)" app.py` retorna ao menos 1 linha
    - `grep -n '"Todos"' app.py` retorna ao menos 2 linhas (uma para CMT, uma para Motorista)
    - `grep -n "st.stop()" app.py` retorna ao menos 1 linha (guard de df vazio)
    - `grep -n "Selecione uma Data e VP" app.py` retorna ao menos 1 linha (instrução D-01)
  </acceptance_criteria>

  <done>app.py com syntax válida, imports corretos (sem data_loader), sidebar com 4 selectboxes em cascata operacionais, botão reload funcional, guard de df vazio, instrução de estado inicial presente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite → app.py via cache_manager | DataFrame já validado pelo Phase 1; confiável para filtros |
| Sidebar inputs → filtered_df | Valores dos selectboxes são sempre subconjuntos do DataFrame — sem injeção possível |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Denial of Service | get_or_process_data() na abertura | accept | Processamento pesado ocorre apenas na primeira execução; barra de progresso já implementada no cache_manager |
| T-02-04 | Spoofing | load_perimeter() FileNotFoundError | mitigate | app.py deve propagar exceção de load_perimeter() sem swallowing — deixar o erro nativo do geo_engine aparecer ao usuário via Streamlit |
</threat_model>

<verification>
1. `python -c "import ast; ast.parse(open('app.py').read()); print('syntax OK')"` — sem erro
2. `grep -c "def " app.py` — retorna 0 (app.py é script, sem funções definidas)
3. `grep -n "st.selectbox" app.py` — retorna 4 linhas (Data, VP, CMT, Motorista)
</verification>

<success_criteria>
- app.py tem syntax Python válida
- Importa exatamente: cache_manager (get_or_process_data, delete_cache), geo_engine (load_perimeter), map_renderer (render_map), streamlit_folium (st_folium)
- NÃO importa data_loader nem geo_engine diretamente para mais nada além de load_perimeter
- 4 selectboxes em cascata presentes, cada um derivando options do slice do anterior
- Guard de df vazio com st.stop() presente
- Instrução "Selecione uma Data e VP" presente no estado inicial
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-interface-streamlit/02-02-SUMMARY.md` com:
- Lista de imports exatos presentes em app.py
- Estrutura de cascata: variáveis usadas em cada nível (df_by_date, df_by_vp, df_by_cmt, filtered_df)
- Confirmação de que st.session_state NÃO é usado para filtros
- Estado do placeholder TODO para Plan 03
</output>
