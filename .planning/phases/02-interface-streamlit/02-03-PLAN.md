---
phase: 02-interface-streamlit
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - app.py
autonomous: false
requirements:
  - UI-02
  - UI-03
  - UI-04
  - UI-05

must_haves:
  truths:
    - "Selecionar Data + VP exibe o mapa Folium com polígono, rota e marcadores coloridos"
    - "Estado inicial (sem VP) exibe mapa vazio centrado no perímetro com instrução de texto"
    - "Pontos INSIDE são verdes, pontos OUTSIDE são vermelhos e maiores, ambos com popup Data_Hora + Endereço"
    - "PolyLine conecta todos os pontos (INSIDE + OUTSIDE) em ordem cronológica"
    - "Título dinâmico acima da tabela: 'VP-XYZ: N registros fora do perímetro' (D-08)"
    - "VP sem OUTSIDE exibe st.success com nome da VP (D-09) — tabela não aparece"
    - "VP com OUTSIDE exibe st.dataframe com 4 colunas: Data_Hora, Endereco, Latitude, Longitude"
    - "fit_bounds no polígono ao inicializar; fit_bounds no polígono + rota ao selecionar VP (D-06)"
    - "Mapa não dispara re-run ao rolar ou dar zoom (returned_objects=[], key='main_map')"
  artifacts:
    - path: "app.py"
      provides: "App Streamlit completo com mapa integrado e tabela OUTSIDE"
      min_lines: 70
  key_links:
    - from: "app.py main area"
      to: "render_map(filtered_df, polygon)"
      via: "if selected_date and selected_vp"
      pattern: "render_map\\(filtered_df, polygon\\)"
    - from: "st_folium call"
      to: "folium.Map"
      via: "returned_objects=[], key='main_map'"
      pattern: "returned_objects=\\[\\]"
    - from: "tabela OUTSIDE"
      to: "filtered_df"
      via: "filtered_df[filtered_df['Status'] == 'OUTSIDE']"
      pattern: "Status.*OUTSIDE"
---

<objective>
Integrar map_renderer ao app.py — substituir o placeholder do Plan 02 pelo mapa Folium real, título dinâmico e tabela de infrações OUTSIDE.

Purpose: Completar a interface: mapa interativo com todas as camadas (polígono, rota, marcadores), título com contagem dinâmica, tabela apenas com registros OUTSIDE, e handling correto de todos os estados (inicial, VP selecionada sem infrações, VP selecionada com infrações).

Output: app.py finalizado. Interface completa e pronta para smoke test.
</objective>

<execution_context>
@C:/Users/hdusa/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hdusa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-interface-streamlit/02-CONTEXT.md
@.planning/phases/02-interface-streamlit/02-RESEARCH.md
@.planning/phases/02-interface-streamlit/02-PATTERNS.md
@.planning/phases/02-interface-streamlit/02-01-SUMMARY.md
@.planning/phases/02-interface-streamlit/02-02-SUMMARY.md

<interfaces>
<!-- Contratos estabelecidos pelos Plans 01 e 02 -->

De map_renderer.py (Plan 01):
```python
def render_map(df: pd.DataFrame, polygon: Polygon) -> folium.Map:
    """
    df vazio → mapa centrado no perímetro apenas (estado inicial D-01/D-06)
    df com dados → mapa com polígono + PolyLine + CircleMarkers + fit_bounds perimetro+rota (D-06)
    """
```

De app.py (Plan 02) — variáveis disponíveis no ponto de integração:
```python
polygon     # shapely.Polygon — carregado no topo do script
filtered_df # DataFrame filtrado — resultado final da cascata
selected_date  # str ou None
selected_vp    # str ou None (ex: "VP-001")
```

st_folium API (streamlit-folium 0.27.2):
```python
st_folium(
    m,                          # folium.Map
    use_container_width=True,   # layout responsivo
    height=500,                 # altura em pixels
    returned_objects=[],        # [] = sem retorno de interação, sem re-run extra
    key="main_map",             # stable key — evita white flash no re-run
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Substituir placeholder por mapa + título dinâmico + tabela OUTSIDE em app.py</name>
  <files>app.py</files>

  <read_first>
    - app.py — localizar o bloco "# TODO Plan 03" para substituição; confirmar variáveis disponíveis (polygon, filtered_df, selected_date, selected_vp)
    - map_renderer.py — confirmar assinatura de render_map(df, polygon) e comportamento com df vazio
    - .planning/phases/02-interface-streamlit/02-PATTERNS.md — seção "Map rendering + table pattern" (código completo do bloco if/else)
    - .planning/phases/02-interface-streamlit/02-RESEARCH.md — Pattern 2 (st_folium), seção "Anti-patterns" (returned_objects, key)
  </read_first>

  <action>
Localizar e substituir o bloco placeholder `# TODO Plan 03` em app.py pelo seguinte bloco completo:

**Bloco de integração (substitui TODO Plan 03):**
```python
# --- Main area ---
if selected_date and selected_vp:
    # Renderizar mapa com rota e marcadores (UI-02, UI-03, UI-04, D-06)
    m = render_map(filtered_df, polygon)
    st_folium(
        m,
        use_container_width=True,
        height=500,
        returned_objects=[],    # evita re-run ao rolar/dar zoom (Pitfall 1 RESEARCH.md)
        key="main_map",         # stable key — evita white flash
    )

    # Tabela de infrações OUTSIDE (UI-05, D-07, D-08, D-09)
    outside_df = filtered_df[filtered_df["Status"] == "OUTSIDE"]

    # Título dinâmico com nome da VP e contagem (D-08)
    st.subheader(f"VP-{selected_vp}: {len(outside_df)} registros fora do perímetro")

    if outside_df.empty:
        # VP sem infrações (D-09)
        st.success(
            f"VP {selected_vp} permaneceu dentro do perímetro em todo o turno."
        )
    else:
        # Tabela com exatamente 4 colunas (D-07)
        st.dataframe(
            outside_df[["Data_Hora", "Endereco", "Latitude", "Longitude"]],
            use_container_width=True,
        )
else:
    # Estado inicial: mapa centrado no perímetro + instrução (D-01, D-06)
    m = render_map(pd.DataFrame(), polygon)
    st_folium(
        m,
        use_container_width=True,
        height=500,
        returned_objects=[],
        key="main_map",
    )
    st.info("Selecione uma Data e VP na sidebar para visualizar a rota.")
```

**Regras críticas:**
- O `else` branch usa `render_map(pd.DataFrame(), polygon)` — DataFrame vazio aciona o modo de estado inicial dentro do render_map (polígono apenas, fit_bounds sobre o polígono)
- `returned_objects=[]` e `key="main_map"` são OBRIGATÓRIOS nas duas chamadas de st_folium
- NÃO passar `location=` nem `zoom_start=` para folium.Map (já gerido pelo render_map via fit_bounds)
- `outside_df[["Data_Hora", "Endereco", "Latitude", "Longitude"]]` — exatamente essas 4 colunas, nessa ordem (D-07)
- `st.success(f"VP {selected_vp} permaneceu...")` — quando outside_df.empty (D-09)
- `st.subheader(f"VP-{selected_vp}: {len(outside_df)} registros...")` — sempre presente quando VP selecionada (D-08)

**Remover a linha placeholder `st.info(f"VP **{selected_vp}** selecionada — mapa será exibido aqui (Plan 03).")`** se estiver presente.
  </action>

  <verify>
    <automated>cd C:/Users/hdusa/Documents/Github/vp-gps && python -c "import ast; ast.parse(open('app.py').read()); print('syntax OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('app.py').read()); print('syntax OK')"` imprime "syntax OK"
    - `grep -n "render_map(filtered_df, polygon)" app.py` retorna ao menos 1 linha
    - `grep -n "render_map(pd.DataFrame(), polygon)" app.py` retorna ao menos 1 linha (estado inicial)
    - `grep -n "returned_objects=\[\]" app.py` retorna ao menos 2 linhas (nas duas chamadas st_folium)
    - `grep -n 'key="main_map"' app.py` retorna ao menos 2 linhas (nas duas chamadas st_folium)
    - `grep -n "height=500" app.py` retorna ao menos 2 linhas
    - `grep -n 'Status.*OUTSIDE\|OUTSIDE.*Status' app.py` retorna ao menos 1 linha (filtro outside_df)
    - `grep -n "st.subheader" app.py` retorna ao menos 1 linha com "registros fora do perímetro"
    - `grep -n "st.success" app.py` retorna ao menos 1 linha com "permaneceu dentro do perímetro"
    - `grep -n '"Data_Hora".*"Endereco".*"Latitude".*"Longitude"' app.py` retorna ao menos 1 linha (tabela 4 colunas D-07)
    - `grep -n "use_container_width=True" app.py` retorna ao menos 3 linhas (nas duas st_folium + st.dataframe)
    - `grep -n "TODO Plan 03\|mapa será exibido aqui" app.py` retorna vazio (placeholder removido)
    - `grep -n "st.info.*Selecione uma Data" app.py` retorna ao menos 1 linha (instrução D-01)
    - `grep -n "outside_df.empty" app.py` retorna ao menos 1 linha (D-09 guard)
  </acceptance_criteria>

  <done>app.py com bloco de integração completo: mapa renderizado em ambos estados, título dinâmico com contagem OUTSIDE, tabela com 4 colunas, st.success para zero infrações, st_folium com returned_objects=[] e key='main_map' em ambas chamadas.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    app.py completo integrado ao map_renderer.py. Executar `streamlit run app.py` e verificar visualmente todos os comportamentos da interface.
  </what-built>
  <how-to-verify>
    1. Executar: `streamlit run app.py` (na pasta do projeto)
    2. **Estado inicial:** O mapa abre mostrando apenas o polígono KML alaranjado, centrado no perímetro. Sidebar mostra 4 selectboxes. Texto "Selecione uma Data e VP na sidebar para visualizar a rota." aparece abaixo do mapa.
    3. **Seleção de Data:** Selecionar uma data na sidebar. Confirmar que o selectbox VP passa a mostrar apenas VPs escaladas naquele dia (não todas as VPs do banco).
    4. **Seleção de VP:** Selecionar uma VP. O mapa deve atualizar exibindo: polígono KML + PolyLine azul (rota) + marcadores verdes/vermelhos. O mapa deve fazer zoom automático para mostrar a rota inteira.
    5. **Marcadores:** Clicar em um marcador verde (INSIDE) — popup deve mostrar Data_Hora e Endereço. Clicar em um marcador vermelho (OUTSIDE) — mesmo popup.
    6. **Tabela:** Título "VP-XXX: N registros fora do perímetro" aparece abaixo do mapa. Se houver infrações: tabela com colunas Data_Hora, Endereço, Latitude, Longitude. Se VP sem infração: caixa verde com texto "VP XXX permaneceu dentro do perímetro em todo o turno."
    7. **Filtros opcionais:** Com Data+VP selecionados, alterar CMT para um valor específico — mapa e tabela devem atualizar. Retornar CMT para "Todos" — mapa volta ao estado VP completo.
    8. **Reload:** Clicar "Recarregar Banco de Dados" — banco é apagado, barra de progresso aparece, dados são reprocessados, app retorna ao estado funcional.
    9. **Zoom do mapa:** Rolar ou dar zoom no mapa — a página NÃO deve recarregar (confirmed por ausência de flash/reload na sidebar).
  </how-to-verify>
  <resume-signal>
    Digite "aprovado" para concluir o Phase 2, ou descreva os problemas encontrados para correção.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filtered_df → st.dataframe | Dados do SQLite local exibidos diretamente; sem escape HTML necessário (st.dataframe trata como texto) |
| folium.Popup HTML | String f-string com dados do DataFrame inseridos diretamente no HTML do popup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | folium.Popup f-string | accept | Dados locais do SQLite; sem PII além de Data_Hora/Endereço; aplicação local sem acesso externo |
| T-02-06 | Tampering | outside_df column slice | accept | Colunas hardcoded no slice — `["Data_Hora", "Endereco", "Latitude", "Longitude"]` — sem possibilidade de exposição de colunas extras |
</threat_model>

<verification>
1. `python -c "import ast; ast.parse(open('app.py').read()); print('syntax OK')"` — sem erro
2. `grep -c "returned_objects=\[\]" app.py` — retorna 2 (nas duas chamadas st_folium)
3. `grep -c 'key="main_map"' app.py` — retorna 2
4. Smoke test visual: `streamlit run app.py` — app abre, filtros funcionam, mapa renderiza
</verification>

<success_criteria>
Fase 2 completa quando:
1. Selecionar Data filtra VPs corretamente na sidebar
2. Selecionar VP exibe mapa com polígono KML + PolyLine azul + marcadores verdes/vermelhos
3. Mapa abre centrado no perímetro no estado inicial
4. Marcadores têm popup com Data_Hora e Endereço
5. Título "VP-XYZ: N registros fora do perímetro" aparece com contagem correta
6. VP sem OUTSIDE: caixa st.success verde exibida, sem tabela
7. VP com OUTSIDE: tabela com exatamente 4 colunas (Data_Hora, Endereco, Latitude, Longitude)
8. Rolar/dar zoom no mapa NÃO recarrega a página
9. Botão "Recarregar Banco de Dados" apaga o banco e reinicia o processamento
</success_criteria>

<output>
Após aprovação do checkpoint, criar `.planning/phases/02-interface-streamlit/02-03-SUMMARY.md` com:
- Confirmação de que app.py está completo e integrado
- Checklist dos 9 critérios de sucesso com status (passou/falhou)
- Qualquer desvio observado no smoke test com resolução
- Estado final dos arquivos criados: map_renderer.py, app.py
</output>
